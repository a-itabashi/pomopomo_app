<%= link_to(musics_play_path(title: trend.title, image_url: trend.image_url, music_url: trend.music_url)) do %>       
  <div class="title_style">
    <%= trend.title %>
  </div>
  <div>
    <%= image_tag(trend.image_url) %>
  </div>
<% end %>